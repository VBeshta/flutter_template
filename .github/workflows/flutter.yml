name: Flutter

on:
  push:
    branches: [ "master", "develop" ]
  pull_request:
    branches: [ "master", "develop" ]


jobs:   #here we specify the jobs - We could have multiple jobs
  build:  #name it something meaningful 
    runs-on: macos-latest   #runner: select a machine to use

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v1
        with:
          java-version: '12.x'
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get 

      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze project source
        run: dart analyze

      - name: Run tests
        run: flutter test

    #   - name: Build android apk and appBundle
    #     run: flutter build apk


    # Follow [article](https://steveos.medium.com/github-action-and-firebase-app-distribution-ci-cd-ways-part-2-fcf9ba425c0) 
    # to generate a Credential file from Google Cloud Console.
    #     # Upload apk to Firebase distribution
    #   - name: Upload apk to Firebase distribution
    #     uses: wzieba/Firebase-Distribution-Github-Action@v1.7.0
    #     with:
    #         appId: ${{ secrets.FIREBASE_APP_ID_ANDROID }}
    #         serviceCredentialsFileContent: ${{ secrets.FIREBASE_TOKEN }}
    #         groups: all_testers
    #         file: build/app/outputs/flutter-apk/app-release.apk

    #   - name: Build android ios
    #     run: flutter build ios --releasee --no-codesign
